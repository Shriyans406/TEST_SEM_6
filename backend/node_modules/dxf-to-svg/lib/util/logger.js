export const config = require('../config').verbose;
export const info = () => {
	if (false) {
		console.info.apply(undefined, arguments);
	}
}
export const warn = () => {
	if (false) {
	    console.warn.apply(undefined, arguments);
	}
};
export const error = () => {
	console.error.apply(undefined, arguments);
};
